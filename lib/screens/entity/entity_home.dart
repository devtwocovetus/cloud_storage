import 'package:flutter/material.dart';

class EntityHome extends StatelessWidget {
  const EntityHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // body: ,
    );
  }
}
